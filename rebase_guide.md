# Git变基操作指南

## 什么是变基(rebase)？

变基是一种Git操作，它可以将一个分支的提交"重新应用"到另一个分支上。这与合并(merge)不同，变基会产生更干净、更线性的提交历史。

## 变基的基本用法

```bash
# 将当前分支变基到main分支
git rebase main

# 交互式变基，可以修改、重排、合并提交
git rebase -i <base_commit>
```

## 变基过程中的常见问题

1. **中断的变基**: 如果变基过程中发生冲突或其他错误，变基会暂停
   - 解决冲突后: `git rebase --continue`
   - 放弃变基: `git rebase --abort`

2. **误操作变基**: 如果误操作了变基命令:
   - 如果变基尚未完成: 执行`git rebase --abort`
   - 如果变基已完成: 使用`git reflog`查看操作历史，然后`git reset --hard`回到变基前的状态

## 变基的最佳实践

1. **不要在公共分支上变基**
   变基会改变提交历史，应当只用于尚未推送到公共仓库的本地提交

2. **变基前先提交或储藏当前更改**
   确保工作区干净再开始变基操作

3. **先在本地测试变基结果**
   确认变基结果符合预期后再推送到远程仓库

## 常用的替代方案

不确定是否应该使用变基时，考虑这些更安全的替代方案:

1. **普通合并**: `git merge <branch>`
2. **拉取**: `git pull` (默认是fetch+merge)
3. **储藏+应用**: `git stash` 然后 `git stash pop`
